# 次回の再開点 —— 機内ブラウザと JS 処理系（2026-09-10）

## いま板に載っている版と、手元にある版

| 板 | 実機で動いている版 | 手元でビルド済み（**未焼き**） |
|---|---|---|
| Pi 5 | **`build Sep 13 2026 15:45:15`**（md5 `c277526b…`, 2.42 MB）CSS＋日本語フォント＋リンク＋言語切替。**実機確認済み** | 同じ（手元と板が一致） |
| Pi 4 | `build Sep 10 2026 06:29:46` | 同じ（変更なし） |
| Pi 3 | 06:33 ビルド（`xinu.boot`） | 同じ（変更なし） |

焼き方は **USB / SD の物理交換のみ**。Pi 5 は USB `XINU5`、Pi 4 は SD `bootfs` の
`kernel8.img`、Pi 3 は SD `XINU` の `kernel.img`（＝`compile/xinu.boot`）。
必ず **md5 照合**と**口の一覧の差分**を取ってから挿す。

## 2026-09-13 起動時の表示を英語版に（未焼き）

実機の画面（`/fb` で取った）では起動時のブラウザ窓が **'.' の列**で埋まっていた ――
取得は起動 10 秒後に自動で通っている（`/browse` が status=10969 を返す）が、
日本語版の本文を機内フォントで描くと全部 '.' に落ちるため。
→ `browser_boot()` と 60 秒ごとの更新を `br_fetch_home()`（**`browser_fetch_en`** →
失敗時は素の取得）に変えた。`?js=1` は実機で未検証なので起動には使っていない。
窓は y=200・高さ 620 に広げた。**焼いて電源を入れ直せば、起動後 10〜20 秒で英語の本文が出る。**

### 14:10:52 を焼いて分かったこと（2026-09-13 実機）
- 起動後 10 秒で **`ok (en)` 3,304 文字**が窓に出た（`/fb` の 3 回連続キャプチャで 39 行分の本文を確認。
  1 回目のキャプチャは描画途中で 3 行しか無かった ―― `/fb` は描画の途中でも読めるので、**1 枚で判断しない**）。
- しかし **60 秒ごとの更新で数回に 1 回 DNS が応答せず**（`-4`）、素の日本語版（'.' の列）や失敗表示に落ちる
  のを 2 分観察して確認（`ok (en)` → `ok` → `ok (en)` → `-4`）。
  → 14:25:45 で (a) 引けた名前を控えて DNS 失敗時はその IP で進む、(b) 英語版が一度出た後は
  更新失敗で上書きせず前回の本文に戻し、note に `update failed: 理由` を残す。
- `?js=1` は新ビルドでも失敗: `記号が合いません near: ':         'Every wait needs a <code>ti`
  （10:08:03 の文字列領域の修正は効いたが、別の箇所で落ちる。辞書値の中の `<code>` か `'` の扱い）。

### 14:25:45 を焼いて分かったこと（2026-09-13 実機・4.5 分観察）
- `update failed: 本文が空です` が出て **前回の英語版が保たれた**（狙いどおり）。
- 起動時の初回は `ok`（日本語）に落ち、60 秒後の更新で `ok (en)` になった → 14:39:14 で起動時は en を 2 回試す。
- **本文の長さが 3,304 → 2,186 → 2,938 と揺れる。** TCP の受信が順序外の段を捨てるだけで、
  **FIN は順序を見ずに受けていた**＝段が 1 つ欠けたまま「完了」。
  → 14:39:14 で (a) FIN は `seq+dlen == rcv_nxt` の時だけ受ける、(b) 順序外の段には同じ ACK を返す（早い再送を誘う）、
  (c) `Content-Length` と本文長を突き合わせ、短ければ `-5 本文が途中で切れました (got/want)` にして表示しない。
  **`text=` の長さが毎回同じ 3,304 になれば直った証拠**（揺れたら段の欠けがまだある）。

### 14:39:14 を焼いて分かったこと（2026-09-13 実機・5.5 分観察）
- **表示は常に `text= 3304`**。起動 10 秒後から英語版で、以後一度も崩れない（保持と検査が効いた）。
- しかし更新の **約半分が失敗**: `i18n.js を取得できません`（辞書 39,242 B のうち 26,800/32,696 で切れ）、
  `本文が途中で切れました (8422/10127)`、`(3446/10127)`。**段の欠けは頻発している**（RX 環は 32 段あるので溢れではないはず）。
  → 14:51:32 で (a) 受信窓を 8192→**2920（2 段）** にして相手のバーストを抑える、(b) 切れた／繋がらない時は
  **その場で 1 回やり直す**、(c) `/browse` の `net=` に **`seg= ooo= finooo= trunc= retry=`** を出す。
  次は `ooo`（順序外の段）が増えるかで「欠けているのか、順序が乱れているだけか」が分かる。

### 14:51:32 を焼いて分かったこと（2026-09-13 実機・6.5 分観察）
- 表示は常に `text= 3304`。更新は 7 回中 5 回成功、失敗は `SYN に応答なし`（trunc→retry の後）と `i18n.js を取得できません`。
- **計器の読み**: 1 回の更新で `seg` +102（≒49 KB ÷ 536 B —— **SYN に MSS を付けていないので相手は 536 B の段で送っていた**）、
  `ooo` +11〜+47（**1〜2 割の段が順序外**）。`finooo=0`。窓を 2920 にしても ooo は減っていない。
  → ooo が「再送された重複（＝こちらの ACK が相手に届いていない）」か「先の段（＝手前が欠けた）」かを分けて数える必要がある。
- 送信側は疑いにくい: `rp1eth_tx_frame` は USED を待つ同期送信（1 バッファでも上書きしない）。RX 環は 32 段。
- 15:01:01 で (a) SYN に **MSS 1460** を付け段数を 1/3 に、(b) 取得の待ちを 8→15 秒（RTO の倍々を吸う）、
  (c) `net=` に **`dup= fut=`**（ooo の内訳）と **`gemrx= gemrre= gemovr=`**（GEM 統計 0x158/0x1A0/0x1A4。読むと 0 に戻るので累計にしてある）。
  `gemrre`/`gemovr` が増えれば板の受信で落ちている。`fut` が主なら手前の段の欠け、`dup` が主ならこちらの ACK 喪失。

### 15:01:01 を焼いて分かったこと（2026-09-13 実機・6 分観察）—— **ここで一区切り**
- **10 回の更新すべて成功**（`trunc=0 retry=0`）。表示は `text= 3304` のまま。
- `seg` +47/回（MSS 1460 が効いて 536 B の段 102 個 → 47 個）。
- `ooo` は **全部 `fut`**（先の段）、`dup=0`、**`gemrre=0 gemovr=0`** ＝ 板の NIC は落としていない、こちらの ACK も届いている。
  **順序外は外（経路）で起きている**（1〜2 割の段が手前より先に届く＝経路上の並べ替えか喪失）。
  536 B 段のときは段数が多く、8 秒の待ちでは再送を吸いきれずに切れていた。MSS 1460＋15 秒で吸える。
- 残る改善余地: 順序外の段を捨てずに buffer に置けば再送待ちが不要になる（今は捨てて相手の再送を待つ）。急がない。

### HTML を解釈して描く（2026-09-13、`system/html.c` 約 600 行・未検証は実機のみ）
- **これまで**は「タグを落として文字を詰める」だけ＝見出しも段落も一塊。**今回**は一回通しの走査で
  「描画命令」(op: 座標・倍率・様式・文字列) を積み、描画は op を並べるだけ。
  ブロック（h1=3倍, h2=2倍, h3=太字+色, p, ul/ol/li, table の行, hr, blockquote, pre）／行内（a=下線+青, b/strong=1px 重ね書き,
  code=琥珀, button/.btn/.chip=`[ ]`, img=`[alt]`, input=`[ ]`）／単語境界の折り返し／実体参照と UTF-8→ASCII 写像（— … “ ” ① τ など）。
  script/style/head/svg の中身は捨てる。CSS は読まない（class は btn/chip/muted/eyebrow/brand だけ見る）。
- **英語版は構造を保って差し替える**: `html_i18n_apply()` が data-i18n 要素の中身（入れ子の深さで閉じタグを特定）を
  辞書の値（行内タグ入り）に置換した HTML を作り、それを整形する。以前の「値を並べる」方式は廃止。
- 表示は `br_present()` を通った文書だけ。**取得の途中経過（辞書・切れた本文）は画面に届かない**ので、前回本文の保持は不要になった。
- スクロール: 窓の中を左クリック、上半分で一画面戻り、下半分で進む（`browser_click`、右端に位置の帯）。
- **Mac 側ハーネス** `scratchpad/html/harness.c`（`html.c`+`font8x8.c` を素の C で組み、PPM に描く／`html_text` を出す／
  `FUZZ=1` で切り詰め・バイト置換・タグ挿入 3,000 通り×ASan+UBSan）。実物の index.html + i18n.js で**見た目を確認してから**焼いた。
  写しを `tools/html_harness.c` に置いた（`cc -o h tools/html_harness.c system/html.c device/video/font8x8.c`）。
- `/browse` の `text=` は `html_text()` の出力（行構造つき）になったので、板の整形結果を Mac から文字で検算できる。

### CSS・日本語フォント・リンク（2026-09-13、html.c を v2 に全面改訂 ≈1,150 行・未検証は実機のみ）
- **フォントは 16 ドット一種**（`device/video/jpfont.c`、生成物 1.2 MB / 実体 ≈280 KB）: ASCII は X11 8x13 を 8x16 の枠に、
  日本語は **X11 jiskan16（JIS X 0208、6,877 字）**。生成は `tools/gen_jpfont.py`（元 BDF も tools/ に置いた）。
  Unicode 順の表を二分探索。`video.c` に `draw_bitmap_glyph()`（任意幅・透過あり）を足した。
  8x8 の旧フォントはもう使わない。英語も 16 ドットで描く（本文 84 桁 × 約 30 行）。
- **CSS**: `<style>` と `<link rel=stylesheet>`（browser.c が取って `html_set_css`）。選択子 tag/.class/#id＋子孫・子。
  擬似クラスの規則は捨てる。性質: color/background(グラデーションは最初の色)/border/font-size(→倍率 1..3)/font-weight/
  text-decoration/text-align/display(none,inline*)/margin/padding(上限 24px)/list-style/text-transform。var()/rgba()/clamp()。
  `color: transparent`（background-clip:text の h1）は色も背景も無視。囲み（bg/border のあるブロック）は閉じるときに
  矩形 op を中身の前へ差し込む。行内の bg（code, chip）は文字ごとの背景。
- **日本語の折り返し**: 全角 1 字 = 1 語。行頭禁則（句読点・閉じ括弧・小書き・長音）は前の字に付け、開き括弧は次の字と結ぶ。
- **言語切替**: `[EN]` ボタン（id=lang-toggle）をクリック → 控えの HTML から組み直す（通信しない）。`/browse?lang=ja|en` でも。
  既定は英語（`br_lang=0`）。日本語は `data-i18n` を差し替えない元の HTML。
- **リンクを辿る**: 文字の op に link 番号を持たせ、クリック点から `html_link_at()`。相対 URL は `br_resolve_url()`。
  **クリックの経路では通信せず** `br_pending_url` に置き、`browser_poll_pending()`（wm の巡回）で取る。https は開けない旨を note に。
  60 秒ごとの更新は「いま表示しているページ」を取り直す。`/browse?url=` も同じ経路（CSS・辞書込み）。
- **Mac で確認済み**: `tools/html_harness.c`（v2）で index.html + i18n.js + style.css を en/ja で描き、目視。
  ASan+UBSan で HTML と CSS の変異 3,000 通り × 2 言語、乱数ブロブ、乱数 CSS、空入力 — すべてクリーン。
  見つけて直した穴: `display: inline-block` の値の先頭の空白（改行）を落としていなかった → 全部ブロック扱いになった。
  「軸」」のように閉じ括弧が行頭に落ちた → 直前の字を wbuf に残して次の句読点を付ける。

### 15:45:15 の実機確認（2026-09-13）
- 起動 10 秒後に `ok (en)` 4,285 文字（CSS の中央揃え・大文字化が `/browse` の文字出力に出ている）。
- `/browse?lang=ja` → `ok (ja)`、`/fb` の画面に **「並列・アクター・型安全を 一つの言語で。」が 3 倍の JIS フォントで**、
  hero の囲み・ボタン・「AIPL とは」・カードの枠が出ている。
- `/browse?url=http://airilab.app/about.html` → 別ページも CSS 込みで整形（リンクを辿る経路と同じ）。
- 未確認: 実機のマウスでリンク／[EN] をクリックしたときの座標合わせ（`browser_click` の top=22+6+26）。
  合っていなければ `html_link_at` の判定幅（±2px）か top を疑う。

## 最初にやること

```bash
# 1. 焼く（USB を挿してから）
cp ~/projects/xinu-rpi5/compile/kernel_2712.img /Volumes/XINU5/kernel_2712.img && sync
md5 -q ~/projects/xinu-rpi5/compile/kernel_2712.img /Volumes/XINU5/kernel_2712.img   # 一致を確認

# 2. 電源再投入のあと
curl 'http://192.168.3.101/browse?js=1' | head -3
```

**期待**: `note= ok (js)` と英語の本文。
**そうならなければ** `note=` の中の `near:` が落ちた箇所を文字で示す。
`/js?src=...` に `usage=` も出るので、領域があふれていないかも同時に見える。

## ここまでで動いているもの

### 機内ブラウザ（`system/browser.c`、約 600 行）
外向き通信の道具が **3 つとも無かった**ので、そこから作った。

1. **ARP 要求と学習** —— 既存 ARP は「要求してきた相手へ返す」受け身だけだった
2. **経路判定** —— 宛先がサブネット外ならゲートウェイの MAC へ送る
3. **TCP の能動オープン** —— 既存 TCP は待ち受けのみ

加えて DNS(UDP/53)、HTTP GET、HTML→文字、窓への描画。

実測（すべて成功）:

| 段階 | 結果 |
|---|---|
| LAN 内（Pi 4） | 201 バイト・ok |
| LAN 外 IP 直指定 | 740 バイト・ok、`gwmac=98:2c:c6:d3:ec:00` |
| **airilab.app（名前）** | **10,973 バイト・本文 3,818 文字** |

口:
```
/browse                    直近の本文 + net= 診断
/browse?url=...            取りに行く
/browse?raw=1              HTML そのまま
/browse?gw=&dns=           経路を実行時に上書き
/browse?en=1               i18n 方式に的を絞った置換（JS を実行しない）
/browse?js=1               **機内 JS 処理系で実行**（本命）
```

### JS 処理系（`system/js.c`、約 800 行）
実機で 10 項目通過（優先順位・文字列連結・アロー関数・入れ子メンバ・
**計算メンバ参照**・typeof・while・三項・**閉包**・配列）。

DOM は **木を作らず区間で持つ**。`data-i18n` 要素の内側の範囲を覚え、
`innerHTML` への代入を差し替えとして記録し、最後に元の HTML を書き出す。
対応するのは `querySelectorAll` / `forEach` / `getAttribute` / `innerHTML` /
`localStorage` / `documentElement` / `addEventListener` の 7 つだけ。

## 残っている作業

1. **10:08:03 を焼いて `/browse?js=1` を確認**（文字列領域の修正が効いたか）
2. 通ったら、実行段で足りない組込みを `near:` と `usage=` を見ながら足す
3. 日本語の表示 —— 機内フォントに字形が無いので現在は `.` に落としている
4. **起動時の自動取得は wm の巡回から駆動している**。約10秒後に最初の取得、
   失敗したら10秒ごとに再試行、通ったら60秒ごとに更新
5. HTTPS は未対応（airilab.app は平文で 200 を返すので今回は不要）

## 踏んだ落とし穴（同じ穴を二度踏まないために）

1. **`js_intern` が満杯で黙って 0 を返していた** —— 途中から全トークンが空文字列になり
   「構文: 記号が合いません」として現れた。**本当の原因はどこにも出ていなかった。**
   → あふれたら失敗させる／重複を潰す／使用量を計器で出す
2. **`proc_sleep_us` で待つと受信が止まる** —— HTTP は `genet_rx_tick()` の**中**で
   処理される。同じ穴への対処が `net_rx_pump()` のコメントに残っていた
   （「応答は板に届いているのに、待ち手が必ず期限切れになる」）
3. **`q_param` は URL 復号をしない** —— `%2B` が生のまま式に入り、全部 `undefined` に
   なった。**「全部おかしい」ときは処理系より先に入口を疑う**
4. **この板は DHCP を arm しない**（静的 IP 192.168.3.101）。`dhcp_get_*` は全部 0
5. **HDMI の有無で `kernel_main` の経路が変わる**。窓の枝では `wm_run()` が
   NULLPROC（idle）として回り、`proc_preempt` は idle を除外するので
   **他の Xinu プロセスが走らない**。専用プロセスは CPU をもらえない
   → `basicwin_poll_pending` / `wifi_adhoc_poll_pending` と同じく wm の巡回から呼ぶ
6. **数える前に作らない** —— 対象の JS が何を使うかを先に数えた（`class`/`await`/`...`
   は全部**文字列の中**だった）。数えずに作れば要らない機能に時間を使っていた
